name: isaiah_miranda_lab_7_sql_singleton_repository_mvvm_getit
description: "A new Flutter project."
publish_to: 'none'
version: 0.1.0+1

environment:
  sdk: ^3.12.0

dependencies:
  flutter:
    sdk: flutter
  get_it: ^9.2.1
  path: ^1.9.1
  provider: ^6.1.5+1
  sqflite: ^2.4.3

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0

flutter:
  uses-material-design: true
